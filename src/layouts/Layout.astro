---
interface Props {
	title: string;
	description: string;
}
import "@styles/global.css";
import { ClientRouter } from "astro:transitions";
import Analytics from '@vercel/analytics/astro'

const { title, description } = Astro.props;

---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/png" href="/favicon.png" />
		<meta name="description" content={description} />
		<meta name="keywords" content="año nuevo, 2026, countdown, celebración, eventos, año nuevo mundial, zonas horarias" />
		<meta name="author" content="tonigt.dev" />
		<title>{title}</title>

		<!-- Open Graph / Facebook -->
		<meta property="og:type" content="website" />
		<meta property="og:url" content="https://www.happynewyear.es/" />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:image" content="https://www.happynewyear.es/og-image.webp" />

		<!-- Twitter -->
		<meta property="twitter:card" content="summary_large_image" />
		<meta property="twitter:url" content="https://www.happynewyear.es/" />
		<meta property="twitter:title" content={title} />
		<meta property="twitter:description" content={description} />
		<meta property="twitter:image" content="https://www.happynewyear.es/og-image.png" />

		<!-- Canonical URL -->
		<link rel="canonical" href="https://www.happynewyear.es/" />

		<!-- Theme Color -->
		<meta name="theme-color" content="#0a0a0a" media="(prefers-color-scheme: dark)" />
		<meta name="theme-color" content="#ffffff" media="(prefers-color-scheme: light)" />

		<!-- Script inline para evitar flash de tema incorrecto (FOUC) -->
		<script is:inline>
			// Este script se ejecuta de forma síncrona antes de renderizar el body
			(function() {
				const getTheme = () => {
					const saved = localStorage.getItem('theme-preference');
					if (saved) return saved;
					return window.matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark';
				};
				
				const theme = getTheme();
				document.documentElement.classList.add(theme);
				document.documentElement.setAttribute('data-theme', theme);
			})();
		</script>

		<!-- Fonts -->
		 <link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400..900&family=Space+Grotesk:wght@300..700&display=swap" rel="stylesheet">
		
		<ClientRouter />
	</head>
	<body>
		<slot />
		<Analytics />
	</body>
</html>

<style is:global>
	:root {
		color-scheme: light dark;
		
		/*Fonts*/
		--primary-font: 'Orbitron', sans-serif;
		--secondary-font: 'Space Grotesk', sans-serif;
	}

	/* Tema oscuro por defecto */
	:root,
	:root.dark {
		--bg: #0a0a0a;
		--text: #ffffff;
		--text60: #ffffff60;
		--gold: #FFD700;
		--gold25: #ffd70040;
		--gold-alt: #FFA500;
	}

	/* Tema claro */
	:root.light {
		--bg: #ffffff;
		--text: #0a0a0a;
		--text60: #0a0a0a60;
		--gold: #DAA520;
		--gold25: #DAA52040;
		--gold-alt: #FF8C00;
	}
	
	html { 
		font-size: 16px; 
		scroll-behavior: smooth; 
		scrollbar-width: thin; 
		scrollbar-color: var(--gold) var(--bg);
	}
	
	body {
		font-family: var(--secondary-font);
		min-height: 100vh;
		max-width: 1920px;
		margin: 0 auto;
		background: radial-gradient(ellipse 80% 60% at 50% 0%, var(--gold25), transparent 70%), var(--bg);
		color: var(--text);
	}

	*::selection {
		background-color: var(--gold);
		color: var(--bg);
	}

	@media (prefers-reduced-motion: reduce) {
		* { 
			animation-duration: 0.01ms !important; 
			animation-iteration-count: 1 !important; 
			transition-duration: 0.01ms !important; 
			scroll-behavior: auto !important;
			transition: none; 
		}
	}
</style>
