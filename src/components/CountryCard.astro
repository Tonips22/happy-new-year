---
interface Props {
    name: string;
    flag: string;
    daysLeft: number;
    hoursLeft: number;
    minutesLeft: number;
    secondsLeft: number;
}
const { name, flag, daysLeft, hoursLeft, minutesLeft, secondsLeft } = Astro.props;
---

<article class="flex items-center justify-between border border-gold rounded-lg p-4 gap-4 hover:border-gold-alt hover:scale-105 transition-all duration-300 cursor-default">
    <div class="flex items-center gap-4">
        <img src={flag} alt={`Bandera de ${name}`}/>
        <h2 class="uppercase font-bold">{name}</h2>
    </div>
    <p class="text-sm text-text60">
        <span class="text-gold-alt">{daysLeft}</span>d
        <span class="text-gold-alt">{hoursLeft}</span>h
        <span class="text-gold-alt">{minutesLeft}</span>m
        <span class="text-gold-alt">{secondsLeft}</span>s
    </p>
</article>