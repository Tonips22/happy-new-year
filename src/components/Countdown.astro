---
// No necesitamos calcular valores iniciales, SimplyCountdown se encarga de todo
---

<!-- Contenedor para SimplyCountdown (vacío, la librería lo llenará) -->
<section id="countdown-target" class="countdown-container"></section>

<div class="mensaje-final" id="mensaje-final" style="display: none;">
  <h2></h2>
</div>

<style>
  .countdown-container {
    display: grid;
    place-content: center;
    grid-template-columns: repeat(1, 1fr);
    gap: 1.5rem;
    max-width: 80%;
    margin: 0 auto;
    padding: 2rem 1rem;

    @media (width >= 768px) {
        grid-template-columns: repeat(4, 1fr);
        gap: 2rem;
    }

    animation: fade-in-down 0.5s ease forwards;
  }

  /* Estilos personalizados para SimplyCountdown */
  .countdown-container :global(.simply-section) {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    min-width: 120px;
    transition: transform 0.150s ease;
    cursor: default;

    @media (width >= 768px) {
        min-width: 150px;
    }
  }

  .countdown-container :global(.simply-section:hover) {
    transform: scale(1.02);
  }

  .countdown-container :global(.simply-amount) {
    font-family: var(--primary-font);
    font-size: 4rem;
    font-weight: 700;
    color: var(--gold);
    line-height: 1;
    transition: color 0.3s ease;
    display: block;
    min-width: 3ch;
    text-align: center;

    @media (width >= 768px) {
        font-size: 5rem;
    }
  }

  .countdown-container :global(.simply-section:hover .simply-amount) {
    color: var(--gold-alt);
  }

  .countdown-container :global(.simply-word) {
    font-family: var(--secondary-font);
    font-size: 1rem;
    color: var(--text);
    opacity: 0.8;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    display: block;
    text-align: center;
    white-space: nowrap;
  }

  .mensaje-final {
    text-align: center;
    padding: 3rem 1rem;

    @media (width <= 768px) {
      min-height: 65vh;
    }
  }

  .mensaje-final h2 {
    font-family: var(--primary-font);
    font-weight: bold;
    font-size: 4rem;
    color: var(--gold);
  }

  @media (prefers-reduced-motion: reduce) {
    .countdown-container :global(.simply-amount) {
      transition: none;
    }
    .mensaje-final h2 {
      animation: none;
    }
    .countdown-container :global(.simply-section:hover) {
      transform: none;
    }
  }
</style>

<script>
  import simplyCountdown from 'simplycountdown.js';
  import Typed from 'typed.js';

  function initCountdown() {
    const countdownElement = document.getElementById('countdown-target');
    
    if (!countdownElement) return;

    // Limpiar cualquier contenido previo (evita duplicados en navegaciones)
    countdownElement.innerHTML = '';

    // Obtener la fecha objetivo: 1 de enero del año siguiente a las 00:00:00
    const currentYear = new Date().getFullYear();
    const nextYear = currentYear + 1;
    // Año siguiente, 1 de enero, 00:00:00 CET (UTC+1)
    const targetDate = new Date(`${nextYear}-01-01T00:00:00+01:00`);
    // Este año, 29 de Noviembre a las 20:10, (UTC+1)
    // const targetDate = new Date(`${currentYear}-11-29T20:10:00+01:00`);

    // Inicializar SimplyCountdown
    simplyCountdown('#countdown-target', {
      year: targetDate.getFullYear(),
      month: targetDate.getMonth() + 1, // JavaScript months son 0-11, SimplyCountdown espera 1-12
      day: targetDate.getDate(),
      hours: 0,
      minutes: 0,
      seconds: 0,

      // Palabras en español con pluralización correcta
      words: {
        days: {
          lambda: (root: string, count: number) => count === 1 ? 'Día' : 'Días',
          root: 'Día'
        },
        hours: {
          lambda: (root: string, count: number) => count === 1 ? 'Hora' : 'Horas',
          root: 'Hora'
        },
        minutes: {
          lambda: (root: string, count: number) => count === 1 ? 'Minuto' : 'Minutos',
          root: 'Minuto'
        },
        seconds: {
          lambda: (root: string, count: number) => count === 1 ? 'Segundo' : 'Segundos',
          root: 'Segundo'
        }
      },

      // Configuración
      plural: true,
      inline: false,
      enableUtc: false, // Usamos hora local (CET/CEST)
      zeroPad: true, // Mostrar 01 en lugar de 1
      countUp: false,
      removeZeroUnits: false,
      refresh: 1000, // Actualizar cada segundo

      // Clases personalizadas (usar las por defecto)
      sectionClass: 'simply-section',
      amountClass: 'simply-amount',
      wordClass: 'simply-word',

      // Callback cuando termina el contador
      onEnd: () => {
        const mensajeFinal = document.getElementById('mensaje-final');
        const countdownContainer = document.getElementById('countdown-target');
        
        if (mensajeFinal && countdownContainer) {
          // Mostrar el contenedor del mensaje
          mensajeFinal.style.display = 'block';
          countdownContainer.remove();

          // Inicializar Typed.js DESPUÉS de mostrar el elemento
          new Typed('#mensaje-final h2', {
            strings: ['¡Feliz Año Nuevo 2027!'],
            loop: false,
            typeSpeed: 200,
            showCursor: false,
          });
        }
      }
    });
  }

  // Inicializar solo una vez al cargar
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initCountdown);
  } else {
    initCountdown();
  }

  // Reinicializar después de navegaciones con View Transitions
  document.addEventListener('astro:page-load', initCountdown);
</script>

<script>
  // Cambiar el título cuando el usuario cambia de pestaña
  const title = document.title;
  const targetDate = new Date(`${new Date().getFullYear() + 1}-01-01T00:00:00+01:00`);

  function getCountdownTitle(): string {
    const diff = targetDate.getTime() - Date.now();
    if (diff <= 0) return '¡Feliz Año Nuevo!';
    const d = Math.floor(diff / 86400000);
    const h = Math.floor((diff % 86400000) / 3600000);
    const m = Math.floor((diff % 3600000) / 60000);
    const s = Math.floor((diff % 60000) / 1000);
    return `${d}d · ${h}h · ${m}m · ${s}s`;
  }

  if ((window as any).__visibilityHandler) {
    document.removeEventListener('visibilitychange', (window as any).__visibilityHandler);
  }
  if ((window as any).__titleInterval) clearInterval((window as any).__titleInterval);

  (window as any).__visibilityHandler = () => {
    if (document.visibilityState === 'hidden') {
      document.title = getCountdownTitle();
      (window as any).__titleInterval = setInterval(() => { document.title = getCountdownTitle(); }, 1000);
    } else {
      clearInterval((window as any).__titleInterval);
      document.title = title;
    }
  };

  document.addEventListener('visibilitychange', (window as any).__visibilityHandler);
</script>